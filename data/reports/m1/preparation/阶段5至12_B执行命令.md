# M1 阶段 5–12：B 顺序执行命令

状态：`preparation_only_not_formal`。以下命令只能在对应门禁通过后执行，不得跳步。

```powershell
$Repo = (Get-Location).Path
$Python = 'python'
if (-not (Test-Path -LiteralPath (Join-Path $Repo 'data-tooling'))) {
  throw '请先进入 Detection-of-Recessive-Advertising 仓库根目录。'
}
Set-Location -LiteralPath $Repo
```

## 0. 随时查看唯一下一步

```powershell
& $Python .\data-tooling\m1_downstream_readiness.py `
  --repo-root $Repo `
  --output .\data\reports\m1\downstream_preparation_status.json
```

该命令只读检查，不锁批、不生成 Gold、不改变任何正式状态。

## 1. A 的 20 条返回后

先把 A/B 文件放到 `data/reports/m1/`，再运行：

```powershell
& $Python .\data-tooling\annotation\validate_qwen_calibration_reviews.py `
  --manifest .\data\reports\m1\qwen_calibration_20_review_manifest.json `
  --review-a .\data\reports\m1\qwen_calibration_20_human_review_A.json `
  --review-b .\data\reports\m1\qwen_calibration_20_human_review_B.json `
  --output .\data\reports\m1\qwen_calibration_20_validation_AB.json
```

复制 `preparation/templates/stage4_model_route_decision_TEMPLATE.json` 为 `data/reports/m1/qwen_calibration_20_human_review_summary.json`，由 A/B 根据真实汇总共同填写。正式状态必须是 `jointly_approved`，并绑定 A/B 两个 SHA。

## 2. 阶段 5：正式锁定四批

运行条件：

- `data/reports/m1/privacy/formal_3312_v2/formalization_report.json` 为 `formal_materialized`；
- `data/reports/m1/privacy/formal_3312_v2/formal_eligible_candidates.jsonl >= 2050`；
- 候选 SHA 与 formalization report 一致；
- A/B 模型路线已共同签字。

```powershell
& $Python .\data-tooling\m1_lock_batches.py `
  --candidates .\data\reports\m1\privacy\formal_3312_v2\formal_eligible_candidates.jsonl `
  --guide .\docs\annotation_guide_v1.md `
  --lock-script .\data-tooling\annotation\lock_annotation_batch.py `
  --output-dir .\data\reports\m1\locked_batches `
  --formalization-report .\data\reports\m1\privacy\formal_3312_v2\formalization_report.json
```

不要添加 `--preflight`。正式成功后由 C 根据 `stage5_C_batch_seal_TEMPLATE.json` 核验并封存。

## 3. 阶段 6：Round 1 B 侧入口

先运行硬预检：

```powershell
& $Python .\data-tooling\m1_round1_preflight.py `
  --batch .\data\reports\m1\locked_batches\round1.jsonl `
  --manifest .\data\reports\m1\locked_batches\round1_manifest.json `
  --guide .\docs\annotation_guide_v1.md `
  --media-base .\data\run_outputs\merged_20260728 `
  --output-dir .\data\run_outputs\m1_annotations\round1\B `
  --report .\data\reports\m1\round1_B_preflight.json
```

预检 `passed=true` 后，才运行任务书中的 B 侧 `manual_review_annotate.py` 命令。完成后定位唯一 B 输出并验收：

```powershell
$Round1B = Get-ChildItem .\data\run_outputs\m1_annotations\round1\B -File -Filter 'B_*.json*' |
  Sort-Object LastWriteTime -Descending | Select-Object -First 1 -ExpandProperty FullName
& $Python .\data-tooling\m1_annotation_output.py `
  --batch .\data\reports\m1\locked_batches\round1.jsonl `
  --manifest .\data\reports\m1\locked_batches\round1_manifest.json `
  --annotation $Round1B --annotator-id B --expected-count 100 `
  --mode human_only `
  --report .\data\reports\m1\annotation_validation\round1_B.json
```

A/B 都完成后计算 `round1_agreement.json`，共同修订指南；C 使用 `stage6_annotation_guide_freeze_TEMPLATE.json` 生成正式 `annotation_guide_freeze_receipt.json`。冻结后不再修改指南。

## 4. 阶段 7：Round 2

A/B 分别纯人工盲标 150 条。每侧完成后使用 `m1_annotation_output.py`，参数改为：

```text
--batch data/reports/m1/locked_batches/round2.jsonl
--manifest data/reports/m1/locked_batches/round2_manifest.json
--expected-count 150
--mode human_only
```

两侧验收通过后计算正式 κ：

```powershell
& $Python .\data-tooling\annotation\calculate_agreement.py `
  <A的唯一Round2输出> <B的唯一Round2输出> `
  --formal-second-round `
  --output .\data\reports\m1\formal_second_round_agreement.json
```

必须同时满足：150 对、`formal_second_round=true`、κ 不低于 0.60。否则停止 Gold，冻结新指南并重新锁一批全新的 150 条。

## 5. 阶段 8：Gold control

A/B 分别纯人工标注 180 条，关闭 AI。验收参数：

```text
--batch data/reports/m1/locked_batches/gold_control.jsonl
--manifest data/reports/m1/locked_batches/gold_control_manifest.json
--expected-count 180
--mode human_only
```

正式验收报告固定为：

```text
data/reports/m1/annotation_validation/gold_control_A.json
data/reports/m1/annotation_validation/gold_control_B.json
```

## 6. 阶段 9：Gold assisted

只有共同模型路线选择 `qwen3.5:4b` 时才启用 Ollama；若选择 `--no-llm`，仍然全部纯人工。每 300 条运行一次部分验收：

```powershell
& $Python .\data-tooling\m1_annotation_output.py `
  --batch .\data\reports\m1\locked_batches\gold_assisted.jsonl `
  --manifest .\data\reports\m1\locked_batches\gold_assisted_manifest.json `
  --annotation <当前A或B输出文件> `
  --annotator-id <A或B> `
  --expected-count 1620 `
  --mode qwen_assisted `
  --allow-partial `
  --report <对应checkpoint报告路径>
```

每次填写 `stage9_300_checkpoint_TEMPLATE.json`。完成 1,620 条后去掉 `--allow-partial`，正式验收报告固定为：

```text
data/reports/m1/annotation_validation/gold_assisted_A.json
data/reports/m1/annotation_validation/gold_assisted_B.json
```

## 7. 阶段 10：裁决与 Gold

先分别计算 control/assisted 的描述性一致性。只把真实 A/B 分歧写入 `data/reports/m1/adjudication.jsonl`，格式参照模板；不得把模板行放入正式文件。

构建 Gold 后必须检查：

- `gold_count >= 1500`；
- 无 `auto_accepted/system`；
- 无同一 annotator 配对；
- 所有实际分歧都有 A+B 裁决或明确排除；
- C 抽查 30–50 条。

## 8. 阶段 11：回连与 creator-safe split

当前 canonical 的 3,312 条均有 `blogger_id`，但 `content_group_id` 全为空。必须先运行真实近重复检测，并用 `stage11_near_duplicate_detection_TEMPLATE.json` 记录算法、阈值、输入 SHA、候选对数量和结论；正式报告路径为 `data/reports/m1/near_duplicate_detection_report.json`。只有检测确实通过时，空 `content_group_id` 才能解释为“未检测到近重复关系”。

先严格回连元数据：

```powershell
& $Python .\data-tooling\m1_gold_metadata.py `
  --gold .\data\gold\gold_v1.jsonl `
  --canonical .\data\run_outputs\merged_20260728\anonymized_posts.jsonl `
  --output .\data\gold\gold_v1_with_groups.jsonl `
  --report .\data\reports\m1\gold_metadata_report.json `
  --minimum-count 1500
```

再运行 `split_by_blogger.py`。只有 creator 和 near-duplicate 泄漏数均为 0，且三份 split 数量之和等于 Gold，才算通过。

## 9. 阶段 12：Dataset Card 与 Gate

A/B 填证据，D 使用 `stage12_D_final_review_TEMPLATE.md` 终审。只有证据真实存在且 D 核验通过，才能逐项把 `dataset_card_status.json` 改为 `true`，最后运行任务书中的 `m1_readiness.py gate`。
